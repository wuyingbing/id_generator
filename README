# Install the dependencies.
sudo apt install python3 python3-pip
pip3 install image tk opencv-python

# Put your avator image to usedres/ directory.
# Scroll to the last of id.py file, modify the ID data to the real value. 
# Run the script.
python3 id.py
